---
title: "01-linux核心基础"
permalink: /linux
sidebar:
  nav: "docs"
---

# linux基础
## 基础命令
cd {目录名(相对路径/绝对路径)}
**解释：**进入目录

mkdir {目录名}
**解释：**创建目录
```
重要参数：
-p 递归创建目录
```

touch {文件名}
**解释：**创建文件，如果文件存在改变时间戳

pwd
**解释：**显示当前目录所在的绝对路径

ls
**解释：**查看当前的目录里面的文件信息
```
主要参数：
-l 查看文件的详细信息（包括但是限于文件权限）
-a 查看隐藏文件
-h 更加友好的查看文件的大小
-d 加上这个就可以看目录了
```

echo {"你想打印的包括参数和语言"}

cat {文件名}
**解释：**查看文件内容
```
特殊的用法：
cat可以跟重定向符号一起使用
例如：cat 文件名 > zzm.txt 如果这个zzm.txt 是没有的那会重新新建一个zzm.txt
如果本来就是有zzm.txt文件的那就是从新覆盖，所以要谨慎使用，如果是不想覆盖的。使用如下命令
cat 文件名 >> zzm.txt 这句画会在末尾追加

cat >> zzm.txt << EOF
这个用法可以帮助多行追加命令
结束符是EOF
```

head {文件名}
**解释：**可以默认打印文件前10行
```
主要参数：
-数字 可以指定查看多少行
```

tail {文件名}
**解释：**可以默认查看末尾后10行
```
主要参数：
-数字 可以指定查看多少行
-f 事实查看主要为了做日志监控
```
